"""Compatibility wrapper.

Use `main.py` + `planner.py` for the submission layout.
This file is kept so existing commands like `python starter_code.py` still work.
"""

from main import main


if __name__ == "__main__":
    main()
